@model List<ArchiveTopic>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h3>🗄️ Arsip</h3>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        ✅ @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<!-- Archived Categories -->
<h5>Arsip Kategori</h5>
<table class="table table-bordered mb-4">
    <thead>
        <tr>
            <th>Kategori</th>
            <th>Deskripsi</th>
            <th>Archived At</th>
            <th>Aksi</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var c in (List<ArchiveCategory>)ViewBag.ArchiveCategories)
    {
        <tr>
            <td>@c.CategoryName</td>
            <td>@c.Description</td>
            <td>@c.ArchivedAt.ToString("dd MMM yyyy")</td>
            <td class="d-flex gap-2">
                <form asp-action="RestoreCategory" method="post">
                    <input type="hidden" name="id" value="@c.ArchiveCategoryId" />
                    <button class="btn btn-success btn-sm">🔄 Restore</button>
                </form>
                <form asp-action="HardDeleteArchiveCategory" method="post">
                    <input type="hidden" name="id" value="@c.ArchiveCategoryId" />
                    <button class="btn btn-danger btn-sm">🗑️ Delete Permanen</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>

<!-- Archived Topics -->
<h5>Arsip Topik</h5>

@* <form method="get" class="mb-3 d-flex gap-2">
    <select name="categoryId" class="form-control" style="max-width:250px">
        <option value="">-- Semua Kategori --</option>
        @foreach (var c in ViewBag.Categories)
        {
            <option value="@c.CategoryId"
                selected="@(ViewBag.CategoryId == c.CategoryId)">
                @c.CategoryName
            </option>
        }
    </select>
    <button class="btn btn-primary">Filter</button>
</form> *@

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Topik</th>
            <th>Kategori</th>
            <th>Views</th>
            <th>Archived At</th>
            <th>Aksi</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var t in Model)
    {
        <tr>
            <td>@t.TopicName</td>
            <td>@t.CategoryName</td>
            <td>@t.ViewCount</td>
            <td>@t.ArchivedAt.ToString("dd MMM yyyy")</td>
            <td class="d-flex gap-2">
                <form asp-action="Restore" method="post">
                    <input type="hidden" name="id" value="@t.ArchiveTopicId" />
                    <button class="btn btn-success btn-sm">🔄 Restore</button>
                </form>
                <form asp-action="HardDeleteArchive" method="post">
                    <input type="hidden" name="id" value="@t.ArchiveTopicId" />
                    <button class="btn btn-danger btn-sm">🗑️ Delete Permanen</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>

<nav>
    <ul class="pagination justify-content-center">
        @for (int i = 1; i <= ViewBag.TotalPages; i++)
        {
            <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                <a class="page-link" href="?page=@i&categoryId=@ViewBag.CategoryId">@i</a>
            </li>
        }
    </ul>
</nav>
